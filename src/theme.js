import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f6ab00',
    },
    secondary: {
      main: '#2da7df',
    },
    tertiary: {
      main: '#c89f64',
    },
  },
});

export default theme;
